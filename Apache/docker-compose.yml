services:
  db:
    image: mysql:8.0
    container_name: apache_mysql_db
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASS}
      MYSQL_DATABASE: ${DB_NAME}
    ports:
      - "3306:3306"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: always

  proxysql:
    image: proxysql/proxysql:latest
    container_name: apache_proxysql
    volumes:
      - ./proxysql.cnf:/etc/proxysql.cnf
    depends_on:
      - db
    restart: always

  redis:
    image: redis:alpine
    container_name: apache_redis
    ports:
      - "6379:6379"
    restart: always

  web:
    build: ./php
    container_name: apache_web
    env_file:
      - .env
    environment:
      DB_HOST: proxysql
      REDIS_HOST: redis
    ports:
      - "8080:80"
    volumes:
      - ../html:/var/www/html
    depends_on:
      - proxysql
      - redis
    restart: always
